SB Admin assets placeholder

Place any template-specific static assets here, for example:
- images/ (logos, backgrounds)
- js/ (optional vendor scripts if not installed via npm)
- vendor/ (3rd-party assets you prefer to ship locally)

Prefer using npm packages for libraries (Chart.js, DataTables, etc.).
